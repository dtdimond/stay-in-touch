  <%= form_for @contact do |f| %>
    <div class="control-group">
      <%= f.label :name %>
      <%= f.text_field :contact_name %>
    </div> 

    <br/>
    <%= f.submit @contact.new_record? ? :Create : :Update, class: "btn btn-primary" %>
  <% end %>
